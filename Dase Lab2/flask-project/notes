clickig on subject
<https://w3id.org/rdfchess/chessonto/Agent>


SELECT DISTINCT ?object
WHERE {
  ?subject ?predicate ?object FILTER(STR(?subject)="https://w3id.org/rdfchess/id/ag1EBk0w")
}


output
<https://w3id.org/rdfchess/chessonto/Agent>


again
SELECT DISTINCT ?subject
WHERE {
  ?subject ?predicate ?object FILTER(STR(?object)="https://w3id.org/rdfchess/chessonto/Agent")
}